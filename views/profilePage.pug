extends layouts/main-layout.pug

block content
    if !profileUser
        span.errorMessage Check the url you are trying to access
    else 
        .profileHeaderContainer 
            .coverPhotoContainer

                .userImageContainer 
                    img(src="/images/" + profilePic, alt="User profile image")
        .postsContainer

block scripts
    script(src="/js/profile.js")